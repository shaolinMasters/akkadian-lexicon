spring:
    application:
        name: Akkadian Lexicon
    profiles:
        active: ${ENVIRONMENT}
registration:
    tokenExpirationDayCount: 1
    confirmationUrlPath: /register
server:
    port: ${WEBSERVER_PORT}





